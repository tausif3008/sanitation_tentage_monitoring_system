<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /kumbh-monitoring/
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /kumbh-monitoring/index.html [L]
</IfModule>
