import React from "react";
import ListMaker from "./ListMaker";

const TasksNotCompletedToday = ({ sanitization, tentage, wastes }) => {
  const messageLost = [];
  const label = "Tasks Not Completed Today";

  return (
    <div>
          <ListMaker ></ListMaker>
    </div>
  );
};

export default TasksNotCompletedToday;
